<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="IceAge.Controls.PollControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IceAge.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ItemsRepeater Grid.Row="0" ItemsSource="{x:Bind ViewModel.PollOptionControls, Mode=OneWay}">
            <ItemsRepeater.Layout>
                <StackLayout Orientation="Vertical" Spacing="5"/>
            </ItemsRepeater.Layout>
        </ItemsRepeater>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,5">
            <TextBlock Text="365 people"/>
            <TextBlock Text="·" Margin="5,0"/>
            <TextBlock Text="6 days left"/>
        </StackPanel>
    </Grid>
</UserControl>
