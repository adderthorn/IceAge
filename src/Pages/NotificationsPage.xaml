<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="IceAge.Pages.NotificationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IceAge.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <ProgressRing Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}" IsIndeterminate="True" Width="100" Height="100"/>
        <ScrollViewer HorizontalScrollMode="Disabled" VerticalScrollMode="Enabled" Padding="0,40,0,0"
                      VerticalScrollBarVisibility="Visible">
            <ItemsControl x:Name="NotificationsPanel" ItemsSource="{x:Bind ViewModel.NotificationControls, Mode=OneWay}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical" Padding="20" CornerRadius="4"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
